name: Build Nicht-Theory Monograph

on:
  workflow_dispatch:

jobs:
  call-workflow-passing-data:
    uses: nicht-organization/nicht-theory/.github/workflows/compile_latex_base.yml@main
    with:
      root-file: master_monograph.tex
